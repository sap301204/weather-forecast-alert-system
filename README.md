# Weather Forecast & Alert Application

Complete end-to-end project: FastAPI backend, provider-agnostic ingestion, alert engine, Streamlit demo, Next.js frontend, Docker & docker-compose.

See /db for schema and seed. Start by following README instructions.
